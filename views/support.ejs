<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />
    <title>Support - <%= appName %></title>
    <meta name="description" content="Support and help resources for <%= appName %>." />

    <link href="https://fonts.googleapis.com/css2?family=Alfa+Slab+One&family=Vend+Sans:wght@400;700&display=swap" rel="stylesheet">

    <style>
        :root{
            --bg: #ffffff;
            --card: #ffffff;
            --card2: #f9fafb;
            --border: #e5e7eb;
            --text: #111827;
            --muted: #4b5563;
            --muted2: #6b7280;
            --accent: #2563EB;
            --accent2: #10b981;
            --warn: #f59e0b;
            --shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
            --radius: 12px;
            --sans: 'Vend Sans', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            --display: 'Alfa Slab One', serif;
            --bg-gradient: linear-gradient(135deg, #f0f4f8 0%, #ffffff 100%);
        }

        * { box-sizing: border-box; }

        body{
            margin:0;
            font-family: var(--sans);
            line-height: 1.6;
            color: var(--text);
            background: var(--bg-gradient);
            background-attachment: fixed;
            padding: 28px 16px 44px;
        }

        a { color: var(--accent); text-decoration: none; }
        a:hover { text-decoration: underline; }

        .wrap{
            max-width: 940px;
            margin: 0 auto;
        }

        .topbar{
            display:flex;
            justify-content:space-between;
            align-items:center;
            gap: 12px;
            margin-bottom: 24px;
            padding: 20px 0;
        }

        .brand{
            display:flex;
            align-items:center;
            gap: 12px;
            text-decoration: none;
            color: inherit;
            cursor: pointer;
        }

        .brand:hover{
            text-decoration: none;
        }

        .logo{
            width: 48px;
            height: 48px;
            border-radius: 14px;
            border: 1px solid var(--border);
            background: #f3f4f6;
            display:flex;
            align-items:center;
            justify-content:center;
            font-size: 1.4rem;
            flex: 0 0 auto;
        }

        .brandtext{
            display:flex;
            flex-direction: column;
            gap: 2px;
            min-width: 0;
        }

        .brand strong{
            font-family: var(--display);
            font-size: 1.4rem;
            font-weight: 400;
            letter-spacing: 0.2px;
            color: var(--text);
        }

        .brand span{
            font-size: 0.82rem;
            color: var(--muted2);
        }

        .card{
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            padding: 34px 28px;
            text-align: center;
        }

        h1{
            margin: 0 0 10px;
            font-family: var(--display);
            font-size: 2rem;
            font-weight: 400;
            letter-spacing: -0.02em;
            color: #111827;
        }

        p{
            margin: 0 0 22px;
            color: var(--muted);
            font-size: 1.05rem;
        }

        .btn{
            display:inline-flex;
            align-items:center;
            justify-content:center;
            gap: 8px;
            padding: 12px 18px;
            border-radius: 14px;
            border: 1px solid var(--border);
            background: #f3f4f6;
            color: var(--text);
            font-weight: 650;
            text-decoration:none;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .btn:hover{
            background: #e5e7eb;
            transform: translateY(-1px);
            text-decoration: none;
        }

        .btn.primary{
            background: var(--accent);
            color: #fff;
            border-color: var(--accent);
        }

        .btn.primary:hover{
            background: #0046CC;
            border-color: #0046CC;
            color: #fff;
        }

        .support-grid{
            display:grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 18px;
            margin-top: 28px;
        }

        .support-box{
            background: #f9fafb;
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 20px 18px;
            text-align: left;
            transition: transform 0.2s;
        }

        .support-box:hover{
            transform: translateY(-2px);
        }

        .support-box h3{
            margin: 0 0 8px;
            font-size: 1.1rem;
            color: var(--text);
        }

        .support-box p{
            font-size: 0.95rem;
            margin-bottom: 14px;
            color: var(--muted);
        }

        .footer{
            text-align:center;
            color: var(--muted2);
            font-size: 0.875rem;
            margin-top: 16px;
            padding: 18px 10px;
        }

        @media(max-width: 520px){
            .card{ padding: 24px 16px; }
            h1{ font-size: 1.6rem; }
        }
    </style>
</head>

<body>
<%
const repo =
        (typeof githubRepo !== "undefined" && githubRepo)
                ? githubRepo
                : "https://github.com/Blackjax-Studio/whats-for-dinner";

const issues = repo + "/issues";
const readme = repo + "#readme";
%>

<div class="wrap">

    <div class="topbar">
        <a href="/" class="brand">
            <div class="logo">🍽️</div>
            <div class="brandtext">
                <strong><%= appName %></strong>
                <span>Support & Help</span>
            </div>
        </a>
    </div>

    <div class="card">
        <h1>Support</h1>

        <p>
            Need help with <strong><%= appName %></strong>?
            This project is maintained as an open-source tool, and support is provided through GitHub.
        </p>

        <div class="support-grid">

            <!-- GitHub Issues -->
            <div class="support-box">
                <h3>🐛 Report a Bug</h3>
                <p>
                    Found something broken or unexpected?
                    The fastest way to get help is by opening a GitHub Issue.
                </p>
                <a href="<%= issues %>" target="_blank" rel="noopener noreferrer" class="btn primary">
                    Open an Issue →
                </a>
            </div>

            <!-- Feature Requests -->
            <div class="support-box">
                <h3>✨ Request a Feature</h3>
                <p>
                    Have an idea that would make the dinner picker even better?
                    Suggestions and pull requests are welcome.
                </p>
                <a href="<%= issues %>" target="_blank" rel="noopener noreferrer" class="btn">
                    Suggest an Improvement →
                </a>
            </div>

            <!-- Documentation -->
            <div class="support-box">
                <h3>📖 Documentation</h3>
                <p>
                    Looking for setup instructions, usage examples, or details about MCP integration?
                    Check the README.
                </p>
                <a href="<%= readme %>" target="_blank" rel="noopener noreferrer" class="btn">
                    View README →
                </a>
            </div>

            <!-- Open Source Notice -->
            <div class="support-box">
                <h3>🌍 Open Source</h3>
                <p>
                    This is a solo open-source project.
                    You can inspect the full codebase anytime for transparency.
                </p>
                <a href="<%= repo %>" target="_blank" rel="noopener noreferrer" class="btn">
                    Visit Repository →
                </a>
            </div>

        </div>
    </div>

    <div class="footer">
        &copy; <%= new Date().getFullYear() %> <%= appName %>.
        Built with ❤️ as an open-source MCP tool.
    </div>

</div>
</body>
</html>
